@*
    @model IEnumerable<INetoMVCApp.Models.INetoTable>
*@
@{
    ViewBag.Title = "INView";
}
<h1>My Users</h1>
<table class="table table-striped table-bordered">
    @foreach (var item in Model)
        {
        <tr>
            <td>@item.UserId</td>
            <td>@item.UserName</td>
            <td>@item.UserEmail</td>
        </tr>}
</table>
